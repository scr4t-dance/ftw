(library
  (name             ftw)
  (public_name      ftw)

  ; ppxs
  (preprocess (pps ppx_deriving_yojson ppx_deriving.enum ppx_deriving.show))

  ; deps
  (libraries
    fmt logs containers sqlite3 sqlite3_utils
  )
)
