(library
  (name             ftw)
  (public_name      ftw)

  ; ppxs
  (preprocess (pps ppx_deriving_yojson))

  ; deps
  (libraries
    unix fmt logs
    gen containers
    sqlite3 sqlite3_utils
    otoml spelll uutf uunf
  )
)
