(library
  (name             ftw)
  (public_name      ftw)

  ; ppxs
  (preprocess (pps ppx_deriving_yojson ppx_deriving.enum ppx_deriving.ord))

  ; deps
  (libraries
    unix fmt logs
    gen containers
    printbox printbox-text
    sqlite3 sqlite3_utils
    otoml spelll uutf uunf
  )
)
