(library
  (name        ftw)
  (public_name ftw)

  ; ppx
  (preprocess
   (pps ppx_deriving.enum))

  ; deps
  (libraries
    ftw_core
    unix fmt logs
    jsont jsont.bytesrw
    gen containers argon2
    printbox printbox-text
    otoml spelll uutf uunf
    fuzzy_match.search ubase
    sqlite3 sqlite3_utils
  )
)
