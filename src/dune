
; Rules for generating the openapi docs
(rule
  (target new_openapi.json)
  (action (run ftw openapi -q %{target})))

(rule
  (alias runtest)
  (package ftw)
  (action (diff openapi.json new_openapi.json)))
